
# Startup
docker swarm init

docker build -t e-shop:build --target runner -f ./server/Dockerfile .

export $(cat .env) > /dev/null 2>&1;docker stack deploy --detach=false -c docker-stack.yml e-shop


# Stop the stack
docker stack rm e-shop


# View logs
docker service logs -f e-shop_server