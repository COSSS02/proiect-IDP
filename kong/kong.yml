_format_version: "3.0"
_transform: true

services:
  - name: client-app
    url: https://tasks.client:5173
    routes:
      - name: client-route
        paths:
          - /
        strip_path: false
        preserve_host: true

  - name: keycloak-auth
    url: http://tasks.keycloak:8080
    routes:
      - name: auth-route
        paths:
          - /auth
        strip_path: false
        preserve_host: true

  - name: adminer-db
    url: http://tasks.adminer:8080
    routes:
      - name: adminer-route
        paths:
          - /adminer
        strip_path: false
        preserve_host: true

  - name: prometheus-monitor
    url: http://tasks.prometheus:9090
    routes:
      - name: prometheus-route
        paths:
          - /prometheus
        strip_path: false

  - name: grafana-dashboard
    url: http://tasks.grafana:3001
    routes:
      - name: grafana-route
        paths:
          - /grafana
        strip_path: false
        preserve_host: true